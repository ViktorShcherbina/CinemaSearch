<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/favorites"
        android:icon="@drawable/ic_favorite"
        android:title="@string/favorite_text"
        app:showAsAction="withText" />

    <item
        android:id="@+id/watch_later"
        android:icon="@drawable/watch_later"
        android:title="@string/later_text"
        app:showAsAction="withText" />

    <item
        android:id="@+id/selections"
        android:icon="@drawable/ic_round_selections"
        android:title="@string/selections_text"
        app:showAsAction="withText" />
</menu>